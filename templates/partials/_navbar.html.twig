<nav class="navbar navbar-expand-lg bg-body-tertiary">
	<div class="container-fluid">
		<a class="navbar-brand" href="/">Holi Sens</a>
		<button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
			<span class="navbar-toggler-icon"></span>
		</button>
		<div class="collapse navbar-collapse" id="navbarSupportedContent">
			<ul class="navbar-nav me-auto mb-2 mb-lg-0">
				<li class="nav-item">
					<a class="nav-link" aria-current="page" href="/">Accueil</a>
				</li>
				<li class="nav-item">
					<a class="nav-link" href="#">Notre Studio</a>
				</li>

				<li class="nav-item">
					<a class="nav-link" href="#">Cours</a>
				</li>
				<li class="nav-item">
					<a class="nav-link" href="#">Retraites</a>
				</li>
				<li class="nav-item">
					<a class="nav-link" href="#">Contact</a>
				</li>
			</ul>
			<div class="d-flex justify-content-end">
				<ul class="navbar-nav me-auto mb-2 mb-lg-0">

			{% if is_granted('ROLE_ADMIN') %}
				<li class="nav-item">
					<a class="nav-link" href="{{ path('admin_backoffice') }}">Back-Office</a>
				</li>
					
			{% endif %}

			{% if app.user %}

					{# <li class="nav-item"><a class="nav-link" href="#">Mon compte</a></li> #}

				<li class="nav-item">
					<a href="{{ path('app_logout') }}" class="btn btn-outline-primary">Déconnexion</a>
				</li>
			{% else %}
					{# <li class="nav-item"><a href="{{ path('app_register') }}" class="btn btn-outline-dark me-3">Inscription</a></li> #}
				<li class="nav-item">
					<a href="{{ path('app_login') }}" class="btn btn-outline-secondary">Connexion</a>
				</li>
			{% endif %}
			</ul></div>
		</div>
	</div>
</nav>
